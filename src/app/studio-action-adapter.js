export function resolveStudioActions(props = {}) {
  const actions = props.actions || {};
  return {
    generateText: actions.generateText || props.onGenerateText || null,
    generateImages: actions.generateImages || props.onGenerateImages || null,
    generateVideos: actions.generateVideos || props.onGenerateVideos || null,
    stopQueue: actions.stopQueue || props.onStopQueue || null,
    openAdvancedCanvas: actions.openAdvancedCanvas || props.onOpenAdvancedCanvas || null,
    openDashboard: actions.openDashboard || props.onOpenDashboard || null,
    openQueue: actions.openQueue || props.onOpenQueue || null,
    openSettings: actions.openSettings || props.onOpenSettings || null,
    openPromptFactory: actions.openPromptFactory || props.onOpenPromptFactory || null,
    openTimeline: actions.openTimeline || props.onOpenTimeline || null,
    runReview: actions.runReview || props.onRunReview || null,
    openExport: actions.openExport || props.onOpenExport || null,
    uploadShotImage: actions.uploadShotImage || props.onUploadShotImage || null,
    uploadShotVideo: actions.uploadShotVideo || props.onUploadShotVideo || null,
    uploadAssetImage: actions.uploadAssetImage || null,
    syncTimelineFromShots: actions.syncTimelineFromShots || null,
    moveTimelineClip: actions.moveTimelineClip || null,
    removeTimelineClip: actions.removeTimelineClip || null,
    updateTimelineClipDuration: actions.updateTimelineClipDuration || null,
    syncTimelineClipMedia: actions.syncTimelineClipMedia || null,
    setTimelineClipReviewStatus: actions.setTimelineClipReviewStatus || null,
    setShotReviewStatus: actions.setShotReviewStatus || null,
    relocateMediaRoot: actions.relocateMediaRoot || null,
    repairLegacyTimelineFromBusiness: actions.repairLegacyTimelineFromBusiness || null,
    repairBusinessTimelineFromLegacy: actions.repairBusinessTimelineFromLegacy || null,
    generateShotImage: actions.generateShotImage || null,
    generateShotVideo: actions.generateShotVideo || null,
    generateAssetImage: actions.generateAssetImage || null,
    retryQueueJobs: actions.retryQueueJobs || null,
    setPrimaryAssetImage: actions.setPrimaryAssetImage || null,
    discardAssetImageCandidate: actions.discardAssetImageCandidate || null,
    exportAssetsAndStoryboard: actions.exportAssetsAndStoryboard || null,
    runFullChainCheck: actions.runFullChainCheck || null,
    runSystemSelfCheck: actions.runSystemSelfCheck || null,
    repairMediaIntegrity: actions.repairMediaIntegrity || null,
    repairAssetConsistency: actions.repairAssetConsistency || null,
    focusMainChain: actions.focusMainChain || null,
    saveProject: actions.saveProject || props.onSaveProject || null,
    openProject: actions.openProject || props.onOpenProject || null,
  };
}
